#include "holberton.h"
#include <stdio.h>

/**
 * main - Entry function
 * @argc: Size of the argv
 * @argv: Array of arguments
 * Return: Return 0
 **/
int main(int  __attribute__((__unused__)) argc, char **argv)
{
	printf("%s\n", (argv[0]));
	return (0);
}
